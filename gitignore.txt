# mutation_check writes these while a mutant is applied, and deletes them
# when it restores. They only survive a crash, and they are the record of
# what was in flight -- useful on the machine that crashed, meaningless
# in the repo. See tests/mutation_check.js.
tests/.mutation-in-flight.json
tests/.mutation-in-flight.bak

# local test scaffolding
node_modules/
